fastapi>=0.110
uvicorn[standard]>=0.29
# jinja2 <3.1.6 has sandbox escapes (GHSA-cpwx-vrp4-4pq7, GHSA-q2x7-8rv6-6q7h,
# GHSA-gmj6-6f8f-6699) and HTML attribute injection (GHSA-h75v-3vvj-5mfj).
jinja2>=3.1.6
# python-multipart <0.0.31 has arbitrary file write (GHSA-wp53-j4wj-2cfg) plus a
# series of multipart/querystring DoS advisories. 0.0.21+ needs Python 3.10, so
# 3.9 pins the highest release it can install and stays exposed to the fixes
# from 0.0.22 on — one more reason to drop 3.9 when Bullseye support ends.
python-multipart>=0.0.31; python_version >= "3.10"
python-multipart>=0.0.20; python_version < "3.10"
pydantic-settings>=2.2
itsdangerous>=2.1
