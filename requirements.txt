# fastapi/uvicorn/pydantic-settings dropped Python 3.9 support in recent releases; keep
# the newest 3.9-compatible floor for that interpreter (CI matrix / Raspberry Pi OS
# Bullseye) and float to latest on 3.10+.
fastapi>=0.141.1; python_version >= "3.10"
fastapi>=0.110,<=0.128.8; python_version < "3.10"
uvicorn[standard]>=0.52.4; python_version >= "3.10"
uvicorn[standard]>=0.29,<=0.39.0; python_version < "3.10"
jinja2>=3.1.6
# python-multipart is pulled in by fastapi; pin explicitly to close known CVEs
# (CVE-2026-24486 arbitrary file write, CVE-2026-53539, CVE-2026-53538, CVE-2026-40347,
# CVE-2026-42561, CVE-2026-53540, CVE-2026-53537 - full list fixed by 0.0.32). It dropped
# Python 3.9 support at 0.0.21, so 0.0.20 is the newest release installable there and
# remains vulnerable to all of the above on that interpreter. (CVE-2024-53981 was already
# fixed by 0.0.18, so it does not apply to the 0.0.20 ceiling.)
python-multipart>=0.0.32; python_version >= "3.10"
python-multipart>=0.0.9,<=0.0.20; python_version < "3.10"
pydantic-settings>=2.15.0; python_version >= "3.10"
pydantic-settings>=2.2,<=2.11.0; python_version < "3.10"
itsdangerous>=2.2
# starlette is pulled in by fastapi; pin explicitly to close known CVEs (CVE-2026-54283
# request.form() size-limit bypass DoS is the most severe, fixed in 1.3.1; also
# CVE-2026-48818, CVE-2026-48710, CVE-2026-48817, CVE-2026-54282). It dropped Python 3.9
# support after 0.49.3, so that is the newest release installable there - it leaves the
# HIGH-severity CVE-2026-54283 (and others above) open on that interpreter. (CVE-2024-47874
# and CVE-2025-54121 were already fixed by 0.40.0 and 0.47.2 respectively, so they do not
# apply to the 0.49.3 ceiling.)
starlette>=1.6.0; python_version >= "3.10"
starlette>=0.36.3,<=0.49.3; python_version < "3.10"
