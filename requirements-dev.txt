-r requirements.txt
# pytest 9 dropped Python 3.9; keep a 3.9-compatible pin for that interpreter so
# the CI matrix (which still covers 3.9 / Raspberry Pi OS Bullseye) stays green.
# Note: CVE-2025-71176 (predictable pytest tmp-dir naming, fixed in 9.0.3) stays open
# on this <9 branch since it can never reach the fix; dev-only dependency, so exploiting
# it requires local/multi-user access to the machine running the test suite.
pytest>=8.4.2,<9; python_version < "3.10"
pytest>=9.1.1; python_version >= "3.10"
httpx>=0.28.1
